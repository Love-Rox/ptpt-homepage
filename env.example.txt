# tcy-homepage environment variables
#
# Server Configuration
PORT=3000

# (No runtime secrets required — this site has no contact form, no Turnstile,
# no Discord webhook. The Worker reads only the ASSETS binding.)
