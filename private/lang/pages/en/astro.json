{
  "title": "@love-rox/ptpt-astro — split-flap integration & components for Astro",
  "hero": {
    "eyebrow": "Astro integration",
    "heading": "@love-rox/ptpt-astro",
    "tagline": "Astro 4+ only. The integration wires up the Markdown plugin, injects the stylesheet, and runs the client hydrator for you. Ships `Patapata.astro` and `PatapataBoard.astro` to drop a display straight into a template.",
    "version": "v0.1.0",
    "isNew": true,
    "ctaNpm": "View on npm",
    "ctaGithub": "GitHub",
    "ctaDemo": "Demo",
    "backToFamily": "← Back to package family"
  },
  "install": {
    "heading": "Install",
    "description": "Peer dependencies: `astro >= 4` and `@love-rox/ptpt-core`. `@love-rox/ptpt-rehype` is pulled in automatically."
  },
  "twoPieces": {
    "heading": "Two pieces",
    "items": [
      {
        "name": "ptpt() integration",
        "description": "Registers `@love-rox/ptpt-rehype` on Astro's Markdown / MDX pipeline, injects `@love-rox/ptpt-core/styles.css`, and runs the client hydrator on every page — so marked content in `.md` / `.mdx` animates with no extra wiring."
      },
      {
        "name": "<Patapata> / <PatapataBoard>",
        "description": "Astro components that emit static, readable markup the hydrator animates on load. `<Patapata>` is one inline display; `<PatapataBoard>` splits on newlines into rows."
      }
    ]
  },
  "usage": {
    "heading": "Usage",
    "description": "Add the integration to `astro.config.mjs`. That alone makes marked Markdown / MDX content animate and injects the stylesheet + hydrator.",
    "integrationLabel": "astro.config.mjs",
    "withOptionsHeading": "With options",
    "withOptionsDescription": "Toggle each piece, and forward rehype options (like `defaultPreset`) through `markdown`.",
    "withOptionsLabel": "astro.config.mjs (with options)",
    "componentHeading": "Inside an `.astro` file",
    "componentDescription": "Drop a display straight into a template with the `<Patapata>` component. `preset` here is a preset name, resolved by the hydrator.",
    "componentLabel": "src/pages/example.astro"
  },
  "options": {
    "heading": "Options",
    "description": "The shared cell options (carried via `data-ptpt-*`) behave exactly as they do in the React / Vue / rehype adapters. The integration itself has three toggles.",
    "sharedLabel": "Shared cell options + rehype passthrough",
    "integrationLabel": "Integration options",
    "columns": {
      "name": "Option",
      "type": "Type",
      "default": "Default",
      "description": "Description"
    }
  },
  "behavior": {
    "heading": "Behavior notes",
    "items": [
      "Astro is SSG-first, so the plugin runs at build time — the only client cost is the small hydrator that animates the static markup.",
      "Without JS the boards still render as plain, readable text; hydration just adds the flip.",
      "`markdown: false` opts out of the Markdown pipeline (e.g. if you only want the components); `styles: false` / `hydrate: false` skip those injections.",
      "The components and the Markdown plugin share the same `data-ptpt-*` output, so `.astro`-authored and Markdown content behave identically."
    ]
  },
  "related": {
    "heading": "Other adapters",
    "description": "Not using Astro? Drop the [rehype plugin](./rehype) into your `unified` pipeline directly. Need runtime control in an app? Use the [React](./react) or [Vue](./vue) components."
  }
}
